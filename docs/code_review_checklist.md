# 原子引擎代码审查清单

## 必查项 (P0)

- [ ] 是否继承 `AtomicEngine` 基类
- [ ] 是否实现 `process()` 方法
- [ ] 是否实现 `get_stats()` 方法
- [ ] 是否有对应的配置文件 (`config/engines/{name}.yaml`)
- [ ] 是否使用 `engine_logger` 而非 `print`
- [ ] 是否有异常处理

## 推荐项 (P1)

- [ ] 是否实现 `reload()` 方法
- [ ] 是否实现 `health_check()` 方法
- [ ] 是否有单元测试
- [ ] 是否有文档字符串

## 优化项 (P2)

- [ ] 是否使用单例模式
- [ ] 是否有类型注解
- [ ] 是否有性能考虑
- [ ] 是否有配置热加载
