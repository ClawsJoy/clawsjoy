# 生产环境密钥配置
jwt:
  secret: ${JWT_SECRET}
  algorithm: HS256
  expiration: 86400

google:
  client_id: ${GOOGLE_CLIENT_ID}
  client_secret: ${GOOGLE_CLIENT_SECRET}

api_keys:
  comfyui: ${COMFYUI_API_KEY}
