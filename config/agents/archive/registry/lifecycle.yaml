# Agent 生命周期配置
version: "3.0.0"

heartbeat_interval: 30
health_check_interval: 60
auto_heal: true
max_retries: 3

states:
  - initializing
  - active
  - idle
  - busy
  - degraded
  - unhealthy
  - stopped
