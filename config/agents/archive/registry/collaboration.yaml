# Agent 协作配置
version: "1.0.0"

# 协作规则
collaboration_rules:
  - intent: ["计算", "数学"]
    primary: "executor_agent"
    fallback: "chat_agent"
  - intent: ["代码", "编程"]
    primary: "code_agent"
    fallback: "chat_agent"

# 超时设置
timeout: 30
retry_count: 2
