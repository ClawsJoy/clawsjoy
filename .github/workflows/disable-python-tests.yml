name: Disable default Python tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
    - name: Skip default Python tests
      run: echo "Default Python tests are disabled"
